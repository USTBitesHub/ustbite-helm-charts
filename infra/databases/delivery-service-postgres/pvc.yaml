apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ustbite-delivery-service-postgres-pvc
  namespace: ustbite-data
  labels:
    app: ustbite-delivery-service-postgres
    component: database
    service: delivery-service
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 1Gi
