apiVersion: v1
kind: Service
metadata:
  name: ustbite-payment-service-postgres
  namespace: ustbite-data
  labels:
    app: ustbite-payment-service-postgres
    component: database
    service: payment-service
spec:
  type: ClusterIP
  selector:
    app: ustbite-payment-service-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
