# THIS IS A TEMPLATE ONLY.
# Do NOT store real credentials here.
# Apply real secrets manually using kubectl:
#
# kubectl create secret generic \
#   ustbite-payment-service-postgres-secret \
#   --namespace ustbite-data \
#   --from-literal=POSTGRES_USER="ustbite_payment" \
#   --from-literal=POSTGRES_PASSWORD="<strong-password>"
#
# Never commit real values to this file.

apiVersion: v1
kind: Secret
metadata:
  name: ustbite-payment-service-postgres-secret
  namespace: ustbite-data
  labels:
    app: ustbite-payment-service-postgres
    component: database
    service: payment-service
type: Opaque
data:
  # base64 encoded placeholders only
  POSTGRES_USER: ""
  POSTGRES_PASSWORD: ""
