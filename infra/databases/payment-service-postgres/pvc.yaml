apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ustbite-payment-service-postgres-pvc
  namespace: ustbite-data
  labels:
    app: ustbite-payment-service-postgres
    component: database
    service: payment-service
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
