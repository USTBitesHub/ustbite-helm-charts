# THIS IS A TEMPLATE ONLY.
# Do NOT store real credentials here.
# Apply real secrets manually using kubectl:
#
# kubectl create secret generic \
#   ustbite-user-service-postgres-secret \
#   --namespace ustbite-data \
#   --from-literal=POSTGRES_USER="ustbite_user" \
#   --from-literal=POSTGRES_PASSWORD="<strong-password>"
#
# Never commit real values to this file.

apiVersion: v1
kind: Secret
metadata:
  name: ustbite-user-service-postgres-secret
  namespace: ustbite-data
  labels:
    app: ustbite-user-service-postgres
    component: database
    service: user-service
type: Opaque
data:
  # base64 encoded placeholders only
  POSTGRES_USER: ""
  POSTGRES_PASSWORD: ""
