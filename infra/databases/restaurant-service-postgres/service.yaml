apiVersion: v1
kind: Service
metadata:
  name: ustbite-restaurant-service-postgres
  namespace: ustbite-data
  labels:
    app: ustbite-restaurant-service-postgres
    component: database
    service: restaurant-service
spec:
  type: ClusterIP
  selector:
    app: ustbite-restaurant-service-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
