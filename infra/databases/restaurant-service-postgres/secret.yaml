# THIS IS A TEMPLATE ONLY.
# Do NOT store real credentials here.
# Apply real secrets manually using kubectl:
#
# kubectl create secret generic \
#   ustbite-restaurant-service-postgres-secret \
#   --namespace ustbite-data \
#   --from-literal=POSTGRES_USER="ustbite_restaurant" \
#   --from-literal=POSTGRES_PASSWORD="<strong-password>"
#
# Never commit real values to this file.

apiVersion: v1
kind: Secret
metadata:
  name: ustbite-restaurant-service-postgres-secret
  namespace: ustbite-data
  labels:
    app: ustbite-restaurant-service-postgres
    component: database
    service: restaurant-service
type: Opaque
data:
  # base64 encoded placeholders only
  POSTGRES_USER: ""
  POSTGRES_PASSWORD: ""
