apiVersion: v1
kind: Service
metadata:
  name: ustbite-order-service-postgres
  namespace: ustbite-data
  labels:
    app: ustbite-order-service-postgres
    component: database
    service: order-service
spec:
  type: ClusterIP
  selector:
    app: ustbite-order-service-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
